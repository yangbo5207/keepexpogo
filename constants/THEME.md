# Shared Theme

This project keeps a single source of truth for design tokens:

- TS runtime tokens: `constants/design-system.ts`
- JSON tokens for cross-project sharing: `constants/design-system.json`
- Tailwind/nativewind variables: `app/global.css`

Use these files for both projects to keep color, spacing, and radius (`xs`) consistent in light/dark mode.

