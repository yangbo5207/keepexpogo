import { View, Text } from 'react-native';

export default function CustomTabInbox() {
  return (
    <View className="flex-1 items-center justify-center bg-cream-50 dark:bg-night-800">
      <Text className="text-3xl">▲</Text>
    </View>
  );
}
